var settings = {
    maxTime: 60000, //max time in ms
    maxScore: 200,
    minScore: 10,
    gracePeriod: 2000,
    QUIZ_CONTENT_TYPE: 'quiz',
    APP_CONTENT_TYPE: 'app'
};

module.exports = settings;
