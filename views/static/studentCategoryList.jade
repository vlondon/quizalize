.navbar.navbar-default.navbar-fixed-top
    .container
        .navbar-header
            a(href="/app")
                img(src="/img/quizalize.png", style="height:44px; margin-top:9px; padding-left:5px;")
        a.pull-right.btn.btn-info(href="/quiz",style="margin-top:9px") Create a quiz in 60 seconds!                        

.container
    br
    h3(ng-hide="quizzes.studentData.studentCode",style="color: white") Here's what quizzes have made so far - lots more quizzes on the way - do you have a suggestion?
    br

    .row
        h1(ng-show="quizzes.studentData.studentCode",style="color: white") Hi {{quizzes.studentData.studentCode}}!

    .row(ng-repeat="category in quizzes.categories | orderBy:quizzes.orderIndex track by $index", style="padding-bottom: 10px")    
        .col-md-12
            .quiz-topic-block                
                a.quiz-link(onclick="return false;" data-toggle="collapse" ng-href="#collapse{{category.category.uuid}}" aria-expanded="false" aria-controls="collapse1")
                    h2.quiz-topic-title.text-center {{category.category.name}}
                .quiz-list.collapse.in(id="collapse{{category.category.uuid}}")
                    .row.quiz-info-row(ng-repeat="quiz in category.quizzes | orderBy:'index' track by $index")
                        .col-xs-9
                            a.quiz-item
                                .quiz-title {{quiz.name}}
                        .col-xs-3
                            .row.quiz-info-row                        
                                button.btn.btn-info.btn-block(clickOnce ng-click="quizzes.startQuiz(category.category.uuid,quiz.uuid);") Play                            
                                

    .col-sm-6.col-sm-offset-3(ng-hide="quizzes.studentData.studentCode==undefined")
        .well
            br
            button.btn.btn-info.btn-block(ng-click="quizzes.reloadQuizzes();")
                span.glyphicon.glyphicon-refresh
                |&nbsp;Check for new quizzes
            br
            a.btn.btn-default.btn-block(ng-click="quizzes.logout();")
                span.glyphicon.glyphicon-user
                | &nbsp;Logout

            h3.text-center(ng-show="quizzes.loading") Fetching Quizzes
